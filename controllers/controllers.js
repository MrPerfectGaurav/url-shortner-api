const {insertOneUrl} = require("./create_shortner/create_shortner");
const {findByFullUrl} = require("./find_by_fullUrl/find_by_fullUrl");

module.exports = {
    insertOneUrl,
    findByFullUrl
};

// Mr. Perfect